# TCM-Flow V1.5 Formal-400 Answer-Level Evaluation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成并冻结 Formal-400 的 B0/B4/P 回答层实验，回答 RQ1、RQ5、RQ6，并在此之后才允许开始天池 TCM-QG 外部验证。

**Architecture:** 回答层不重新执行检索，而是只读复用已经冻结的 Formal dev/test 逐题检索结果。先在 `formal_dev` 冻结生成模型、结构化输出 Prompt、B4/P 无答案阈值和人工审核抽样规则，再通过一次冻结的 `run-formal-answer-test` 执行 `formal_test` 的 B0/B4/P 三组主比较方法和 P-no-parent 回答消融，每题三次生成；全量使用确定性指标，预注册样本使用盲审证据支持标签。

**Tech Stack:** Python 3.10、Pydantic 2、PyYAML、LangChain `ChatOpenAI`、OpenAI-compatible API、现有 Formal-400 Manifest 和检索运行结果、`unittest`

---

## 新对话交接

新对话从本计划开始，不重新执行已完成的 Formal-400 检索，也不开始天池实验。

当前冻结状态：

```text
formal_dataset_frozen=true
formal_retrieval_completed=true
formal_report_written=true
answer_level_evaluation_completed=false
next_stage=B0/B4/P answer-level evaluation
```

已冻结输入：

```text
Formal dataset SHA256:
1C344CB271B24D14366AA0389AE192FDA99388D51724E56260D50497D7902230

Formal test retrieval matrix:
data/rag_v1_5/formal/runs/test/
formal_test-20260615T102626Z-1C344CB2-D832EF32

Formal dev retrieval matrix:
data/rag_v1_5/formal/runs/dev/
formal_dev-20260615T100221Z-1C344CB2-D832EF32

Frozen retrieval manifest:
experiments/rag_v1_5/manifests/formal-runs-v1.5.0.json
```

执行本计划时必须保持：

- 不修改 `formal-400.jsonl`；
- 不修改 Formal Chunk、索引、检索配置和检索结果；
- 不根据 `formal_test` 调整 Prompt、阈值或审核抽样；
- 不把私有问题、参考答案、Evidence 正文或生成答案提交到 Git；
- 回答层结果冻结后，才进入天池外部验证计划。

## 文件结构

新增：

```text
experiments/rag_v1_5/configs/formal-answer.yaml
experiments/rag_v1_5/formal_answer.py
experiments/rag_v1_5/answer_metrics.py
experiments/rag_v1_5/answer_review.py
tests/rag_v1_5/test_formal_answer.py
tests/rag_v1_5/test_answer_metrics.py
tests/rag_v1_5/test_answer_review.py
docs/experiments/v1.5-formal-answer-results.md
```

修改：

```text
experiments/rag_v1_5/schema.py
experiments/rag_v1_5/cli.py
experiments/rag_v1_5/README.md
experiments/rag_v1_5/EXPERIMENT_LOG.md
```

本地忽略产物：

```text
data/rag_v1_5/formal/answer/dev/
data/rag_v1_5/formal/answer/test/
data/rag_v1_5/formal/answer/review/
```

可提交冻结清单：

```text
experiments/rag_v1_5/manifests/formal-answer-prereg-v1.5.0.json
experiments/rag_v1_5/manifests/formal-answer-runs-v1.5.0.json
```

## 固定实验定义

回答配置：

| 配置 | 输入 |
|---|---|
| B0 | 只提供问题，不提供检索证据 |
| B4 | 复用 `b4-c0-hybrid-rerank` 的冻结 Top-5 |
| P | 复用 `p-c4-hybrid-rerank` 的冻结 Top-5 Parent context |

每题每配置生成 `3` 次。测试集的一次性运行指一个冻结矩阵命令；三次重复在该命令内部完成。

结构化输出：

```json
{
  "answer": "回答正文",
  "abstain": false,
  "citations": ["E1", "E2"]
}
```

无答案输出：

```json
{
  "answer": "在指定古籍证据范围内未找到可靠答案。",
  "abstain": true,
  "citations": []
}
```

主要比较：

```text
B4 vs B0 -> RQ1
P vs B4 -> 完整方法的回答层效果
P vs p-no-parent answer ablation -> RQ5
B0/B4/P 的拒答准确率与无证据错误回答率 -> RQ6
```

`p-no-parent` 不作为第四套独立检索配置重新运行。它复用 P 的完全相同排序，只把输入上下文由 Parent 切换为 Child，从而隔离 Parent recovery。

## Task 1: 定义回答层配置和数据契约

**Files:**
- Create: `experiments/rag_v1_5/configs/formal-answer.yaml`
- Modify: `experiments/rag_v1_5/schema.py`
- Create: `tests/rag_v1_5/test_formal_answer.py`

- [ ] **Step 1: 写配置契约失败测试**

在 `tests/rag_v1_5/test_formal_answer.py` 添加：

```python
import unittest

from pydantic import ValidationError

from experiments.rag_v1_5.schema import (
    FormalAnswerOutput,
    FormalAnswerRunRecord,
)


class FormalAnswerSchemaTests(unittest.TestCase):
    def test_output_requires_consistent_abstention_and_citations(self):
        valid = FormalAnswerOutput(
            answer="桂枝汤。",
            abstain=False,
            citations=["E1"],
        )
        self.assertEqual(valid.citations, ["E1"])

        with self.assertRaises(ValidationError):
            FormalAnswerOutput(
                answer="未找到。",
                abstain=True,
                citations=["E1"],
            )

    def test_run_record_has_frozen_method_repeat_and_usage(self):
        record = FormalAnswerRunRecord(
            question_id="formal-q-001",
            split="formal_dev",
            method="P",
            repeat_index=0,
            answer="桂枝汤。",
            abstain=False,
            citations=["E1"],
            retrieval_gate_abstain=False,
            evidence_ids=["E1"],
            latency_ms=123.0,
            input_tokens=100,
            output_tokens=10,
            model_name="frozen-model",
        )
        self.assertEqual(record.method, "P")
```

- [ ] **Step 2: 运行测试并确认失败**

Run:

```powershell
.\.venv\Scripts\python.exe -m unittest `
  tests.rag_v1_5.test_formal_answer.FormalAnswerSchemaTests -v
```

Expected: FAIL，原因是回答层 Schema 尚不存在。

- [ ] **Step 3: 实现最小 Schema**

在 `experiments/rag_v1_5/schema.py` 增加：

```python
from typing import Literal


class FormalAnswerOutput(BaseModel):
    model_config = ConfigDict(extra="forbid")

    answer: str = Field(min_length=1)
    abstain: bool
    citations: list[str] = Field(default_factory=list)

    @model_validator(mode="after")
    def validate_abstention(self) -> "FormalAnswerOutput":
        if self.abstain and self.citations:
            raise ValueError("拒答时 citations 必须为空")
        if not self.abstain and any(
            not citation.startswith("E") for citation in self.citations
        ):
            raise ValueError("citation 必须使用 E1、E2 格式")
        return self


class FormalAnswerRunRecord(BaseModel):
    model_config = ConfigDict(extra="forbid")

    question_id: str = Field(min_length=1)
    split: Literal["formal_dev", "formal_test"]
    method: Literal["B0", "B4", "P", "P-no-parent"]
    repeat_index: int = Field(ge=0, le=2)
    answer: str = Field(min_length=1)
    abstain: bool
    citations: list[str]
    retrieval_gate_abstain: bool
    evidence_ids: list[str]
    latency_ms: float = Field(ge=0)
    input_tokens: int = Field(ge=0)
    output_tokens: int = Field(ge=0)
    model_name: str = Field(min_length=1)
```

- [ ] **Step 4: 创建固定回答配置**

创建 `experiments/rag_v1_5/configs/formal-answer.yaml`：

```yaml
version: v1.5.0
seed: 20260616

model:
  env_model_key: OPENAI_MODEL
  env_base_url_key: OPENAI_BASE_URL
  temperature: 0.2
  max_tokens: 512
  timeout_seconds: 120
  max_retries: 2
  structured_output_method: json_mode

generation:
  repeats: 3
  context_top_k: 5
  answer_methods: [B0, B4, P, P-no-parent]
  evidence_labels: [E1, E2, E3, E4, E5]

retrieval_sources:
  B4: b4-c0-hybrid-rerank
  P: p-c4-hybrid-rerank
  P-no-parent: p-no-parent

no_answer:
  score_field: reranker_score
  objective: balanced_accuracy
  tie_break: higher_threshold
  fallback_when_score_missing: model_only

evaluation:
  automatic_metrics:
    - exact_match
    - char_f1
    - citation_precision
    - citation_recall
    - refusal_accuracy
    - unsupported_answer_rate
    - latency_ms
    - input_tokens
    - output_tokens
  bootstrap_seed: 20260616
  bootstrap_resamples: 10000
  confidence_level: 0.95

human_review:
  answerable_questions: 80
  unanswerable_questions: 40
  canonical_repeat_index: 0
  second_review_rate: 0.10
  sample_seed: 20260616
```

- [ ] **Step 5: 运行 Schema 测试**

Run:

```powershell
.\.venv\Scripts\python.exe -m unittest `
  tests.rag_v1_5.test_formal_answer.FormalAnswerSchemaTests -v
```

Expected: PASS。

- [ ] **Step 6: 提交**

```powershell
git add experiments/rag_v1_5/schema.py `
  experiments/rag_v1_5/configs/formal-answer.yaml `
  tests/rag_v1_5/test_formal_answer.py
git commit -m "test: 定义Formal回答层契约"
```

## Task 2: 冻结回答层预注册 Manifest

**Files:**
- Create: `experiments/rag_v1_5/formal_answer.py`
- Modify: `experiments/rag_v1_5/cli.py`
- Modify: `tests/rag_v1_5/test_formal_answer.py`

- [ ] **Step 1: 写预注册冻结失败测试**

添加：

```python
def test_freeze_answer_prereg_records_only_hashes_and_model_identity(
    self,
):
    from experiments.rag_v1_5.formal_answer import (
        freeze_formal_answer_prereg,
    )

    manifest = freeze_formal_answer_prereg(
        config_path=self.root / "formal-answer.yaml",
        formal_manifest_path=self.root / "formal-400.json",
        formal_runs_manifest_path=self.root / "formal-runs.json",
        dev_run_dir=self.root / "dev-run",
        test_run_dir=self.root / "test-run",
        output_path=self.root / "answer-prereg.json",
        env={
            "OPENAI_MODEL": "frozen-model",
            "OPENAI_BASE_URL": "https://example.invalid/v1",
        },
    )
    serialized = json.dumps(manifest, ensure_ascii=False)

    self.assertEqual(manifest["status"], "ready")
    self.assertEqual(manifest["model"]["name"], "frozen-model")
    self.assertNotIn("api_key", serialized.lower())
    self.assertNotIn("reference_answer", serialized)
```

测试夹具必须创建最小 `ready` Manifest、配置文件以及 dev/test 目录中的 `matrix-config.json`。

- [ ] **Step 2: 运行测试并确认失败**

Run:

```powershell
.\.venv\Scripts\python.exe -m unittest `
  tests.rag_v1_5.test_formal_answer.FormalAnswerSchemaTests.test_freeze_answer_prereg_records_only_hashes_and_model_identity -v
```

Expected: FAIL，函数尚不存在。

- [ ] **Step 3: 实现预注册冻结**

在 `formal_answer.py` 实现：

```python
def freeze_formal_answer_prereg(
    *,
    config_path: Path,
    formal_manifest_path: Path,
    formal_runs_manifest_path: Path,
    dev_run_dir: Path,
    test_run_dir: Path,
    output_path: Path,
    env: Mapping[str, str] | None = None,
) -> dict:
    environment = os.environ if env is None else env
    config = yaml.safe_load(config_path.read_text(encoding="utf-8"))
    model_name = environment.get(config["model"]["env_model_key"], "").strip()
    base_url = environment.get(config["model"]["env_base_url_key"], "").strip()
    if not model_name or not base_url:
        raise ValueError("回答层要求冻结 OPENAI_MODEL 和 OPENAI_BASE_URL")

    manifest = {
        "version": "v1.5.0",
        "status": "ready",
        "stage": "formal_answer_preregistered",
        "model": {
            "name": model_name,
            "base_url_origin": urlsplit(base_url).netloc,
        },
        "inputs": {
            "config_sha256": _sha256_file(config_path),
            "formal_manifest_sha256": _sha256_file(
                formal_manifest_path
            ),
            "formal_runs_manifest_sha256": _sha256_file(
                formal_runs_manifest_path
            ),
            "dev_matrix_config_sha256": _sha256_file(
                dev_run_dir / "matrix-config.json"
            ),
            "test_matrix_config_sha256": _sha256_file(
                test_run_dir / "matrix-config.json"
            ),
        },
        "methods": config["generation"]["answer_methods"],
        "repeats": config["generation"]["repeats"],
        "test_policy": "single_frozen_matrix",
    }
    _atomic_write_json(output_path, manifest)
    return manifest
```

复用 `runner.py` 中 `_atomic_write_json` 和 `_sha256_file`，不要复制另一套散列实现。

- [ ] **Step 4: 增加 CLI**

在 `cli.py` 增加常量：

```python
DEFAULT_FORMAL_ANSWER_CONFIG_PATH = Path(
    "experiments/rag_v1_5/configs/formal-answer.yaml"
)
DEFAULT_FORMAL_ANSWER_PREREG_PATH = Path(
    "experiments/rag_v1_5/manifests/"
    "formal-answer-prereg-v1.5.0.json"
)
DEFAULT_FORMAL_DEV_MATRIX_DIR = Path(
    "data/rag_v1_5/formal/runs/dev/"
    "formal_dev-20260615T100221Z-1C344CB2-D832EF32"
)
DEFAULT_FORMAL_TEST_MATRIX_DIR = Path(
    "data/rag_v1_5/formal/runs/test/"
    "formal_test-20260615T102626Z-1C344CB2-D832EF32"
)
```

添加 `freeze-formal-answer-prereg` 子命令，参数包括 `--config`、`--formal-manifest`、`--formal-runs-manifest`、`--dev-run-dir`、`--test-run-dir` 和 `--output`。

- [ ] **Step 5: 运行测试**

Run:

```powershell
.\.venv\Scripts\python.exe -m unittest tests.rag_v1_5.test_formal_answer -v
```

Expected: PASS。

- [ ] **Step 6: 冻结真实预注册**

Run:

```powershell
.\.venv\Scripts\python.exe -m experiments.rag_v1_5.cli `
  freeze-formal-answer-prereg
```

Expected:

```text
status=ready
stage=formal_answer_preregistered
methods=B0,B4,P,P-no-parent
repeats=3
```

- [ ] **Step 7: 提交**

```powershell
git add experiments/rag_v1_5/formal_answer.py `
  experiments/rag_v1_5/cli.py `
  experiments/rag_v1_5/manifests/formal-answer-prereg-v1.5.0.json `
  tests/rag_v1_5/test_formal_answer.py
git commit -m "feat: 冻结Formal回答层预注册"
```

## Task 3: 加载冻结检索结果并构造证据上下文

**Files:**
- Modify: `experiments/rag_v1_5/formal_answer.py`
- Modify: `tests/rag_v1_5/test_formal_answer.py`

- [ ] **Step 1: 写冻结输入与上下文测试**

添加：

```python
def test_loads_only_frozen_b4_p_and_child_contexts(self):
    from experiments.rag_v1_5.formal_answer import (
        load_frozen_answer_inputs,
    )

    loaded = load_frozen_answer_inputs(
        dataset_path=self.dataset_path,
        matrix_dir=self.matrix_dir,
        answer_prereg_path=self.answer_prereg_path,
        split="formal_dev",
    )

    self.assertEqual(set(loaded["retrieval"]), {"B4", "P", "P-no-parent"})
    self.assertEqual(
        loaded["retrieval"]["P"]["q-1"]["evidence"][0]["label"],
        "E1",
    )
    self.assertEqual(
        loaded["retrieval"]["P-no-parent"]["q-1"]["evidence"][0]["text"],
        "child text",
    )
```

- [ ] **Step 2: 运行测试并确认失败**

Run:

```powershell
.\.venv\Scripts\python.exe -m unittest `
  tests.rag_v1_5.test_formal_answer.FormalAnswerSchemaTests.test_loads_only_frozen_b4_p_and_child_contexts -v
```

Expected: FAIL。

- [ ] **Step 3: 实现只读加载**

实现：

```python
ANSWER_RETRIEVAL_CONFIGS = {
    "B4": "b4-c0-hybrid-rerank",
    "P": "p-c4-hybrid-rerank",
    "P-no-parent": "p-no-parent",
}


def build_evidence_items(record: dict, top_k: int = 5) -> list[dict]:
    items = []
    for index, hit in enumerate(record["hits"][:top_k], start=1):
        items.append(
            {
                "label": f"E{index}",
                "text": hit["context_text"],
                "chunk_id": hit["chunk_id"],
                "clause_ids": hit["clause_ids"],
                "reranker_score": hit.get("reranker_score"),
            }
        )
    return items
```

`load_frozen_answer_inputs` 必须：

- 校验 answer prereg 为 `ready`；
- 校验数据集、Formal Manifest、retrieval Manifest 和 matrix-config 哈希；
- 校验 split 与目录一致；
- 每个配置恰好 200 个唯一 question ID；
- 只读取 B4、P、P-no-parent；
- 不重新调用检索器。

- [ ] **Step 4: 增加上下文去重**

P 的 Top-5 若多次恢复到同一个 Parent，按 `retrieval_parent_id` 去重，保留最高排名。B4 按 `chunk_id` 去重。输出标签在去重后重新编号为 `E1...En`。

测试：

```python
def test_parent_context_deduplicates_by_retrieval_parent_id(self):
    items = build_evidence_items(self.duplicate_parent_record)
    self.assertEqual([item["label"] for item in items], ["E1", "E2"])
    self.assertEqual(len(items), 2)
```

- [ ] **Step 5: 运行测试**

Run:

```powershell
.\.venv\Scripts\python.exe -m unittest tests.rag_v1_5.test_formal_answer -v
```

Expected: PASS。

- [ ] **Step 6: 提交**

```powershell
git add experiments/rag_v1_5/formal_answer.py `
  tests/rag_v1_5/test_formal_answer.py
git commit -m "feat: 加载Formal冻结检索证据"
```

## Task 4: 实现固定 Prompt 和模型适配器

**Files:**
- Modify: `experiments/rag_v1_5/formal_answer.py`
- Modify: `tests/rag_v1_5/test_formal_answer.py`

- [ ] **Step 1: 写 Prompt 和模型调用失败测试**

添加：

```python
def test_prompt_requires_evidence_only_answer_and_citations(self):
    from experiments.rag_v1_5.formal_answer import build_answer_messages

    messages = build_answer_messages(
        question="桂枝汤由哪些药组成？",
        evidence=[{"label": "E1", "text": "桂枝、芍药、生姜..."}],
        method="P",
    )
    serialized = json.dumps(messages, ensure_ascii=False)
    self.assertIn("只能依据给定证据", serialized)
    self.assertIn('"citations"', serialized)
    self.assertIn("E1", serialized)


def test_b0_prompt_has_no_evidence_labels(self):
    messages = build_answer_messages(
        question="桂枝汤由哪些药组成？",
        evidence=[],
        method="B0",
    )
    self.assertNotIn("[E1]", json.dumps(messages, ensure_ascii=False))
```

- [ ] **Step 2: 运行测试并确认失败**

Run:

```powershell
.\.venv\Scripts\python.exe -m unittest `
  tests.rag_v1_5.test_formal_answer.FormalAnswerSchemaTests.test_prompt_requires_evidence_only_answer_and_citations -v
```

Expected: FAIL。

- [ ] **Step 3: 实现 Prompt**

系统 Prompt 固定为：

```text
你是中医古籍证据问答评测模型。
对于 B4、P 和 P-no-parent，只能依据给定证据回答，不得补充证据之外的医学常识。
证据不足时必须拒答。
引用只能使用输入中给出的 E1、E2 等标签。
输出必须符合 JSON：answer、abstain、citations。
拒答时 answer 固定为“在指定古籍证据范围内未找到可靠答案。”，
abstain=true，citations=[]。
```

B0 系统 Prompt 使用相同输出契约，但明确“不提供外部证据，可依据模型已有知识回答；不确定时拒答；citations 必须为空”。

- [ ] **Step 4: 实现可注入模型适配器**

```python
class FormalAnswerModel:
    def __init__(self, *, config: dict) -> None:
        self.model_name = os.environ[
            config["model"]["env_model_key"]
        ]
        model = ChatOpenAI(
            model=self.model_name,
            base_url=os.environ[
                config["model"]["env_base_url_key"]
            ],
            temperature=config["model"]["temperature"],
            max_tokens=config["model"]["max_tokens"],
            timeout=config["model"]["timeout_seconds"],
            max_retries=config["model"]["max_retries"],
        )
        self.structured = model.with_structured_output(
            FormalAnswerOutput,
            method=config["model"]["structured_output_method"],
            include_raw=True,
        )

    def invoke(self, messages: list[dict]) -> tuple[FormalAnswerOutput, dict]:
        response = self.structured.invoke(messages)
        parsed = FormalAnswerOutput.model_validate(response["parsed"])
        metadata = getattr(response["raw"], "response_metadata", {}) or {}
        usage = metadata.get("token_usage", {})
        return parsed, {
            "input_tokens": int(usage.get("prompt_tokens", 0)),
            "output_tokens": int(usage.get("completion_tokens", 0)),
            "system_fingerprint": metadata.get("system_fingerprint"),
        }
```

测试必须使用 Fake Model，不访问网络。

- [ ] **Step 5: 运行测试**

Run:

```powershell
.\.venv\Scripts\python.exe -m unittest tests.rag_v1_5.test_formal_answer -v
```

Expected: PASS。

- [ ] **Step 6: 提交**

```powershell
git add experiments/rag_v1_5/formal_answer.py `
  tests/rag_v1_5/test_formal_answer.py
git commit -m "feat: 实现Formal回答Prompt与模型适配"
```

## Task 5: 在 formal_dev 校准拒答阈值并冻结 Prompt

**Files:**
- Modify: `experiments/rag_v1_5/formal_answer.py`
- Modify: `experiments/rag_v1_5/cli.py`
- Modify: `tests/rag_v1_5/test_formal_answer.py`

- [ ] **Step 1: 写阈值校准失败测试**

添加：

```python
def test_calibrates_threshold_on_dev_only_with_stable_tie_break(self):
    from experiments.rag_v1_5.formal_answer import calibrate_threshold

    rows = [
        {"answerable": True, "score": 0.9},
        {"answerable": True, "score": 0.8},
        {"answerable": False, "score": 0.4},
        {"answerable": False, "score": 0.2},
    ]
    result = calibrate_threshold(rows)
    self.assertEqual(result["objective"], "balanced_accuracy")
    self.assertGreater(result["threshold"], 0.4)
    self.assertLessEqual(result["threshold"], 0.8)
```

- [ ] **Step 2: 运行测试并确认失败**

Run:

```powershell
.\.venv\Scripts\python.exe -m unittest `
  tests.rag_v1_5.test_formal_answer.FormalAnswerSchemaTests.test_calibrates_threshold_on_dev_only_with_stable_tie_break -v
```

Expected: FAIL。

- [ ] **Step 3: 实现阈值校准**

候选阈值取排序后相邻分数中点，以及边界值。预测规则：

```python
predicted_answerable = score >= threshold
```

优化 `balanced_accuracy`；并列时选择更高阈值，降低无证据错误回答风险。B4 和 P 分别校准；P-no-parent 与 P 使用相同阈值，因为排序和分数相同。

- [ ] **Step 4: 实现 dev 运行**

增加：

```text
run-formal-answer-dev
freeze-formal-answer-dev
```

`run-formal-answer-dev`：

- 只接受 `formal_dev`；
- 运行 B0/B4/P/P-no-parent，每题三次；
- 支持相同输入哈希下断点恢复；
- 逐条写 `per-answer.jsonl` 和 `errors.jsonl`；
- API 密钥、问题正文和答案不写入可提交 Manifest。

`freeze-formal-answer-dev`：

- 从冻结 retrieval score 校准 B4/P 阈值；
- 冻结系统 Prompt 和用户模板 SHA256；
- 记录模型名、base URL 域名、温度、Token 上限；
- 记录 200 题 × 4 配置 × 3 次的完成情况；
- 输出到本地 `data/rag_v1_5/formal/answer/dev/dev-freeze.json`。

- [ ] **Step 5: 增加 CLI 测试**

```python
def test_answer_cli_contracts(self):
    from experiments.rag_v1_5.cli import build_parser

    dev = build_parser().parse_args(["run-formal-answer-dev"])
    freeze = build_parser().parse_args(["freeze-formal-answer-dev"])
    self.assertEqual(dev.command, "run-formal-answer-dev")
    self.assertEqual(freeze.command, "freeze-formal-answer-dev")
```

- [ ] **Step 6: 运行单元测试**

Run:

```powershell
.\.venv\Scripts\python.exe -m unittest tests.rag_v1_5.test_formal_answer -v
```

Expected: PASS。

- [ ] **Step 7: 执行真实 dev 运行**

Run:

```powershell
.\.venv\Scripts\python.exe -m experiments.rag_v1_5.cli `
  run-formal-answer-dev

.\.venv\Scripts\python.exe -m experiments.rag_v1_5.cli `
  freeze-formal-answer-dev
```

Expected:

```text
split=formal_dev
question_count=200
method_count=4
repeats=3
expected_runs=2400
error_count=0
prompt_frozen=true
b4_threshold_frozen=true
p_threshold_frozen=true
```

- [ ] **Step 8: 提交代码，不提交私有 dev 结果**

```powershell
git add experiments/rag_v1_5/formal_answer.py `
  experiments/rag_v1_5/cli.py `
  tests/rag_v1_5/test_formal_answer.py
git commit -m "feat: 冻结Formal回答层开发参数"
```

## Task 6: 实现一次性 formal_test 回答矩阵

**Files:**
- Modify: `experiments/rag_v1_5/formal_answer.py`
- Modify: `experiments/rag_v1_5/cli.py`
- Modify: `tests/rag_v1_5/test_formal_answer.py`

- [ ] **Step 1: 写测试集门禁失败测试**

添加：

```python
def test_test_run_requires_frozen_dev_and_rejects_second_fresh_run(self):
    from experiments.rag_v1_5.formal_answer import run_formal_answer_matrix

    with self.assertRaises(ValueError):
        run_formal_answer_matrix(
            split="formal_test",
            dev_freeze_path=self.root / "missing.json",
            output_dir=self.root / "test",
            model_factory=self.fake_model_factory,
        )
```

并测试已有 completed test 目录时，未传 `resume_dir` 的第二次 fresh run 必须拒绝。

- [ ] **Step 2: 运行测试并确认失败**

Run:

```powershell
.\.venv\Scripts\python.exe -m unittest `
  tests.rag_v1_5.test_formal_answer.FormalAnswerSchemaTests.test_test_run_requires_frozen_dev_and_rejects_second_fresh_run -v
```

Expected: FAIL。

- [ ] **Step 3: 实现测试矩阵**

`run_formal_answer_matrix` 必须：

- 校验 answer prereg 和 dev freeze 哈希；
- 校验 split=`formal_test`；
- 加载恰好 200 个 approved 问题；
- 使用冻结 Prompt、模型参数和阈值；
- B4/P 在分数低于阈值时直接输出固定拒答；
- 其余调用模型；
- B0 不使用检索阈值；
- 每个问题、方法、repeat 唯一；
- 保存错误但不中途吞掉错误；
- 只有进程中断时允许相同哈希 `--resume`；
- 完成后写 `matrix-summary.json`。

- [ ] **Step 4: 增加测试命令**

在 CLI 增加：

```text
run-formal-answer-test
```

默认输出：

```text
data/rag_v1_5/formal/answer/test/
```

- [ ] **Step 5: 运行单元测试**

Run:

```powershell
.\.venv\Scripts\python.exe -m unittest tests.rag_v1_5.test_formal_answer -v
```

Expected: PASS。

- [ ] **Step 6: 一次性运行 formal_test**

只有 dev freeze 完成并审计后执行：

```powershell
.\.venv\Scripts\python.exe -m experiments.rag_v1_5.cli `
  run-formal-answer-test
```

Expected:

```text
split=formal_test
question_count=200
method_count=4
repeats=3
expected_runs=2400
error_count=0
status=completed
```

不得为了改善指标重跑。只有意外中断时：

```powershell
$answerMatrixDir = (
  Get-ChildItem data/rag_v1_5/formal/answer/test -Directory |
  Sort-Object LastWriteTime |
  Select-Object -Last 1
).FullName

.\.venv\Scripts\python.exe -m experiments.rag_v1_5.cli `
  run-formal-answer-test `
  --resume $answerMatrixDir
```

- [ ] **Step 7: 提交代码**

```powershell
git add experiments/rag_v1_5/formal_answer.py `
  experiments/rag_v1_5/cli.py `
  tests/rag_v1_5/test_formal_answer.py
git commit -m "feat: 实现Formal回答层测试矩阵"
```

## Task 7: 实现自动回答指标

**Files:**
- Create: `experiments/rag_v1_5/answer_metrics.py`
- Create: `tests/rag_v1_5/test_answer_metrics.py`
- Modify: `experiments/rag_v1_5/cli.py`

- [ ] **Step 1: 写指标失败测试**

创建：

```python
import unittest

from experiments.rag_v1_5.answer_metrics import (
    char_f1,
    citation_metrics,
    refusal_metrics,
)


class AnswerMetricsTests(unittest.TestCase):
    def test_char_f1_normalizes_chinese_punctuation(self):
        self.assertEqual(char_f1("桂枝、芍药", "桂枝芍药"), 1.0)

    def test_citation_metrics_use_gold_clause_coverage(self):
        result = citation_metrics(
            citations=["E1", "E2"],
            evidence={
                "E1": {"clause_ids": ["gold-1"]},
                "E2": {"clause_ids": ["other"]},
            },
            gold_clause_ids=["gold-1"],
        )
        self.assertEqual(result["precision"], 0.5)
        self.assertEqual(result["recall"], 1.0)

    def test_refusal_metrics_distinguish_correct_refusal(self):
        result = refusal_metrics(
            answerable=False,
            abstain=True,
        )
        self.assertEqual(result["correct"], 1)
```

- [ ] **Step 2: 运行测试并确认失败**

Run:

```powershell
.\.venv\Scripts\python.exe -m unittest tests.rag_v1_5.test_answer_metrics -v
```

Expected: FAIL。

- [ ] **Step 3: 实现确定性指标**

实现：

```python
def normalize_answer(text: str) -> str:
    normalized = unicodedata.normalize("NFKC", text)
    return "".join(
        char for char in normalized
        if not char.isspace()
        and not unicodedata.category(char).startswith(("P", "S"))
    )
```

计算：

- exact match；
- 中文字符多重集合 F1；
- citation precision；
- citation recall；
- answerable 正确回答率；
- unanswerable 正确拒答率；
- unsupported answer rate；
- latency 和 Token；
- 三次重复的均值、标准差和逐题稳定性。

`unsupported_answer_rate` 的自动定义只允许是：

```text
unanswerable 且 abstain=false
```

不得把字符串不匹配直接称为幻觉。回答中陈述是否受证据支持由人工审核决定。

- [ ] **Step 4: 增加汇总 CLI**

添加：

```text
summarize-formal-answer-test
```

输出到测试矩阵目录：

```text
automatic-metrics.json
per-question-metrics.jsonl
paired-bootstrap.json
```

Bootstrap 以 question ID 为重采样单位，每次同时携带该题所有方法和三个 repeat。

- [ ] **Step 5: 运行测试**

Run:

```powershell
.\.venv\Scripts\python.exe -m unittest tests.rag_v1_5.test_answer_metrics -v
```

Expected: PASS。

- [ ] **Step 6: 汇总真实结果**

```powershell
$answerMatrixDir = (
  Get-ChildItem data/rag_v1_5/formal/answer/test -Directory |
  Sort-Object LastWriteTime |
  Select-Object -Last 1
).FullName

.\.venv\Scripts\python.exe -m experiments.rag_v1_5.cli `
  summarize-formal-answer-test `
  --run-dir $answerMatrixDir
```

- [ ] **Step 7: 提交**

```powershell
git add experiments/rag_v1_5/answer_metrics.py `
  experiments/rag_v1_5/cli.py `
  tests/rag_v1_5/test_answer_metrics.py
git commit -m "feat: 计算Formal回答层自动指标"
```

## Task 8: 建立盲审样本和人工证据审核

**Files:**
- Create: `experiments/rag_v1_5/answer_review.py`
- Create: `tests/rag_v1_5/test_answer_review.py`
- Modify: `experiments/rag_v1_5/cli.py`

- [ ] **Step 1: 写抽样与盲化失败测试**

创建：

```python
import unittest

from experiments.rag_v1_5.answer_review import build_review_sample


class AnswerReviewTests(unittest.TestCase):
    def test_review_sample_is_blinded_and_stratified(self):
        rows = build_review_sample(
            questions=self.questions,
            answers=self.answers,
            seed=20260616,
            answerable_count=80,
            unanswerable_count=40,
            canonical_repeat_index=0,
        )
        self.assertEqual(len(rows), 360)
        self.assertNotIn("method", rows[0])
        self.assertIn(rows[0]["blind_method"], {"A", "B", "C"})
```

主比较表为 360 行，来自 120 个问题 × B0/B4/P。RQ5 专项表使用同一批 80 道 answerable 问题比较 P/P-no-parent，共 160 行。两张表合计 520 行，第二审核固定抽取 10%，即 52 行。

增加专项表测试：

```python
def test_parent_ablation_review_has_paired_160_rows(self):
    rows = build_review_sample(
        questions=self.questions,
        answers=self.answers,
        seed=20260616,
        answerable_count=80,
        unanswerable_count=40,
        canonical_repeat_index=0,
    )
    parent_rows = [
        row for row in rows
        if row["review_track"] == "parent_ablation"
    ]
    self.assertEqual(len(parent_rows), 160)
```

- [ ] **Step 2: 运行测试并确认失败**

Run:

```powershell
.\.venv\Scripts\python.exe -m unittest tests.rag_v1_5.test_answer_review -v
```

Expected: FAIL。

- [ ] **Step 3: 实现固定抽样**

抽取：

- formal_test 全部 40 道 unanswerable；
- 80 道 answerable，按两本书和四种 answerable 题型分层；
- 每种方法只使用 `repeat_index=0` 进入人工审核；
- B0/B4/P 方法映射对每个问题随机打乱并保存到本地私有 key；
- P/P-no-parent 对同一批 80 道 answerable 建立独立配对盲审表；
- 第二审核固定抽取全部 520 行的 10%，即 52 行。

- [ ] **Step 4: 定义审核字段**

主审核 CSV：

```text
review_id
question_id
book_scope
question_type
question
reference_answer
evidence
answer
blind_method
answer_correct
evidence_support
citation_correct
appropriate_refusal
hallucination
review_status
review_comment
second_review_required
second_review_status
```

枚举：

```text
answer_correct: yes / partial / no
evidence_support: full / partial / none / not_applicable
citation_correct: yes / partial / no / not_applicable
appropriate_refusal: yes / no / not_applicable
hallucination: yes / no
review_status: pass / revise / exclude
```

审核判断只基于冻结问题、参考答案和返回证据，不要求评价临床疗效。

- [ ] **Step 5: 实现导入门禁**

`import_formal_answer_review` 必须：

- 防止 question、answer、evidence、blind_method 被修改；
- 所有首审字段填写完整；
- second review 样本填写完整；
- 分歧保留，不静默覆盖；
- 输出一致性统计和待裁决行。

- [ ] **Step 6: 增加 CLI**

```text
prepare-formal-answer-review
import-formal-answer-review
```

- [ ] **Step 7: 运行测试**

Run:

```powershell
.\.venv\Scripts\python.exe -m unittest tests.rag_v1_5.test_answer_review -v
```

Expected: PASS。

- [ ] **Step 8: 生成并完成人工审核**

```powershell
$answerMatrixDir = (
  Get-ChildItem data/rag_v1_5/formal/answer/test -Directory |
  Sort-Object LastWriteTime |
  Select-Object -Last 1
).FullName

.\.venv\Scripts\python.exe -m experiments.rag_v1_5.cli `
  prepare-formal-answer-review `
  --run-dir $answerMatrixDir
```

人工审核完成后：

```powershell
.\.venv\Scripts\python.exe -m experiments.rag_v1_5.cli `
  import-formal-answer-review
```

- [ ] **Step 9: 提交代码，不提交审核正文**

```powershell
git add experiments/rag_v1_5/answer_review.py `
  experiments/rag_v1_5/cli.py `
  tests/rag_v1_5/test_answer_review.py
git commit -m "feat: 建立Formal回答层盲审"
```

## Task 9: 冻结回答层结果 Manifest

**Files:**
- Modify: `experiments/rag_v1_5/formal_answer.py`
- Modify: `experiments/rag_v1_5/cli.py`
- Modify: `tests/rag_v1_5/test_formal_answer.py`

- [ ] **Step 1: 写隐私安全 Manifest 失败测试**

添加：

```python
def test_freeze_answer_runs_excludes_private_text(self):
    from experiments.rag_v1_5.formal_answer import (
        freeze_formal_answer_runs,
    )

    manifest = freeze_formal_answer_runs(
        run_dir=self.test_run_dir,
        review_summary_path=self.review_summary_path,
        answer_prereg_path=self.answer_prereg_path,
        output_path=self.root / "formal-answer-runs.json",
    )
    serialized = json.dumps(manifest, ensure_ascii=False)
    self.assertEqual(manifest["status"], "ready")
    self.assertNotIn("question", serialized)
    self.assertNotIn("answer_text", serialized)
    self.assertNotIn("context_text", serialized)
```

- [ ] **Step 2: 运行测试并确认失败**

Run:

```powershell
.\.venv\Scripts\python.exe -m unittest `
  tests.rag_v1_5.test_formal_answer.FormalAnswerSchemaTests.test_freeze_answer_runs_excludes_private_text -v
```

Expected: FAIL。

- [ ] **Step 3: 实现冻结**

Manifest 只包含：

- 输入 Manifest 和配置哈希；
- Prompt SHA256；
- 模型名、base URL 域名、生成参数；
- 方法、题数、重复数、成功/失败数；
- 自动指标；
- 人工审核聚合指标与一致性；
- 文件相对路径和 SHA256；
- `answer_level_evaluation_completed=true`；
- `next_stage=TCM-QG external validation`。

- [ ] **Step 4: 增加 CLI**

```text
freeze-formal-answer-runs
```

- [ ] **Step 5: 运行测试**

Run:

```powershell
.\.venv\Scripts\python.exe -m unittest tests.rag_v1_5.test_formal_answer -v
```

Expected: PASS。

- [ ] **Step 6: 冻结真实结果**

```powershell
$answerMatrixDir = (
  Get-ChildItem data/rag_v1_5/formal/answer/test -Directory |
  Sort-Object LastWriteTime |
  Select-Object -Last 1
).FullName

.\.venv\Scripts\python.exe -m experiments.rag_v1_5.cli `
  freeze-formal-answer-runs `
  --run-dir $answerMatrixDir
```

Expected:

```text
status=ready
answer_level_evaluation_completed=true
next_stage=TCM-QG external validation
```

- [ ] **Step 7: 提交**

```powershell
git add experiments/rag_v1_5/formal_answer.py `
  experiments/rag_v1_5/cli.py `
  experiments/rag_v1_5/manifests/formal-answer-runs-v1.5.0.json `
  tests/rag_v1_5/test_formal_answer.py
git commit -m "data: 冻结Formal回答层结果"
```

## Task 10: 写回答层报告并关闭 Formal-400

**Files:**
- Create: `docs/experiments/v1.5-formal-answer-results.md`
- Modify: `experiments/rag_v1_5/EXPERIMENT_LOG.md`
- Modify: `experiments/rag_v1_5/README.md`

- [ ] **Step 1: 写正式报告**

报告至少包含：

1. 阶段边界和冻结输入；
2. B0/B4/P/P-no-parent 定义；
3. Prompt、模型与生成参数；
4. dev 阈值校准；
5. test 一次性运行和错误数；
6. EM、字符 F1、引用 Precision/Recall；
7. 拒答准确率和 unsupported answer rate；
8. 人工 evidence support、hallucination 和一致性；
9. B4-B0、P-B4、P-(P-no-parent) 配对区间；
10. 延迟和 Token 成本；
11. 失败案例分类；
12. RQ1、RQ5、RQ6 判定；
13. 自建片段锚定题库局限；
14. 下一阶段为天池外部验证。

不得将自动字符串指标称为医学正确性，不得把未经人工审核的内容称为幻觉。

- [ ] **Step 2: 追加实际实验日志**

在 `EXPERIMENT_LOG.md` 记录：

- 实际命令；
- 输入、配置、Prompt、模型和输出哈希；
- dev 阈值；
- test matrix ID；
- API 错误和恢复情况；
- 自动与人工指标；
- 结论边界；
- `answer_level_evaluation_completed=true`；
- `next_stage=TCM-QG external validation`。

- [ ] **Step 3: 更新 README**

增加回答层命令：

```powershell
.\.venv\Scripts\python.exe -m experiments.rag_v1_5.cli freeze-formal-answer-prereg
.\.venv\Scripts\python.exe -m experiments.rag_v1_5.cli run-formal-answer-dev
.\.venv\Scripts\python.exe -m experiments.rag_v1_5.cli freeze-formal-answer-dev
.\.venv\Scripts\python.exe -m experiments.rag_v1_5.cli run-formal-answer-test
.\.venv\Scripts\python.exe -m experiments.rag_v1_5.cli summarize-formal-answer-test
.\.venv\Scripts\python.exe -m experiments.rag_v1_5.cli prepare-formal-answer-review
.\.venv\Scripts\python.exe -m experiments.rag_v1_5.cli import-formal-answer-review
.\.venv\Scripts\python.exe -m experiments.rag_v1_5.cli freeze-formal-answer-runs
```

- [ ] **Step 4: 检查隐私**

Run:

```powershell
rg -n "reference_answer|context_text|question_text|answer_text" `
  experiments/rag_v1_5/manifests/formal-answer-*.json
git status --short
```

Expected: Manifest 中没有私有正文字段；`data/rag_v1_5` 未被跟踪。

- [ ] **Step 5: 提交**

```powershell
git add docs/experiments/v1.5-formal-answer-results.md `
  experiments/rag_v1_5/EXPERIMENT_LOG.md `
  experiments/rag_v1_5/README.md
git commit -m "docs: 总结Formal回答层实验"
```

## Task 11: 全量验证和天池阶段门禁

**Files:**
- Modify only if verification finds a scoped defect.

- [ ] **Step 1: 运行回答层测试**

```powershell
.\.venv\Scripts\python.exe -m unittest `
  tests.rag_v1_5.test_formal_answer `
  tests.rag_v1_5.test_answer_metrics `
  tests.rag_v1_5.test_answer_review -v
```

Expected: 全部 PASS。

- [ ] **Step 2: 运行全量测试和编译**

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -p "test*.py"
.\.venv\Scripts\python.exe -m compileall -q app experiments tests
.\.venv\Scripts\python.exe -m pip check
git diff --check
```

Expected:

```text
unittest=OK
compileall=passed
pip_check=No broken requirements found
git_diff_check=passed
```

- [ ] **Step 3: 核对最终阶段字段**

```powershell
Get-Content `
  experiments/rag_v1_5/manifests/formal-answer-runs-v1.5.0.json |
  Select-String `
    "answer_level_evaluation_completed|next_stage|status"
```

Expected:

```text
status=ready
answer_level_evaluation_completed=true
next_stage=TCM-QG external validation
```

- [ ] **Step 4: 核对工作区隐私**

```powershell
git ls-files data/rag_v1_5
git status --short
```

Expected: `git ls-files data/rag_v1_5` 无输出；私有回答数据未暂存。

- [ ] **Step 5: 提交最终审计**

```powershell
git add experiments/rag_v1_5/EXPERIMENT_LOG.md
git commit -m "docs: 完成Formal回答层实验审计"
```

## 完成判定

只有同时满足以下条件，Formal-400 才算全部完成：

```text
formal_dataset_frozen=true
formal_retrieval_completed=true
formal_report_written=true
answer_prereg_status=ready
answer_dev_frozen=true
answer_test_completed=true
answer_review_completed=true
answer_level_evaluation_completed=true
formal_answer_report_written=true
```

此时下一阶段才允许变为：

```text
next_stage=TCM-QG external validation
```

随后新建独立实施计划执行：

```text
docs/superpowers/specs/
2026-06-15-tcm-flow-v1.5-external-tcm-parent-child-validation-design.md
```
